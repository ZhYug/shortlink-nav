{
  "name": "st-nav",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ST Nav — Cloudflare Pages Advanced Mode + D1",
  "scripts": {
    "dev": "npm run build && wrangler pages dev public",
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler pages deploy public",
    "db:migrate:local": "npm run build && wrangler d1 migrations apply st-nav --local",
    "db:migrate:remote": "node scripts/migrate-remote.mjs",
    "doctor": "node scripts/doctor.mjs"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
