name = "st-nav"
pages_build_output_dir = "./public"
compatibility_date = "2026-09-11"

# D1 is intentionally bound in the Cloudflare Pages Dashboard.
# The repository does not store a database_id.
#
# Dashboard:
# Settings → Bindings → Add → D1 database
# Variable name: DB

[observability]
enabled = true

[vars]
ST_NAV_VERSION = "1.0.0"
