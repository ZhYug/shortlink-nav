# Cloudflare Pages configuration
# Pages Git integration can read this file directly, so the first deployment
# does not need a Build command or Build output directory entered in the dashboard.

name = "st-nav"
pages_build_output_dir = "./public"
compatibility_date = "2026-09-11"
keep_vars = true
